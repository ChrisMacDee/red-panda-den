routes:
  /:
    template: home

collections:
  /blog/:
    permalink: /blog/{slug}/
    template: index
    filter: tag:-portfolio

  /portfolio/:
    permalink: /portfolio/{slug}/
    template: portfolio
    filter: tag:portfolio

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
